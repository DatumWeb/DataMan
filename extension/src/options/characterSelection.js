/**
 * Future: list characters, create/switch active, tune physics.
 * Background remains source of truth via runtime.sendMessage.
 */
(function (O) {
  O.characterSelection = {
    init() {
      /* Placeholder — wire to GET_STATE / SET_ACTIVE_CHARACTER in a later slice */
    }
  };
})(globalThis.DataManOptions);
